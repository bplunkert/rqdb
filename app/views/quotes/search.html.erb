<%= form_with(url: '/search', local: true) do |form| %>
  <div class="field">
    <%= form.text_area :search_pattern %>
  </div>

  <div class="actions">
    <%= form.submit 'Search' %>
  </div>
<% end %>