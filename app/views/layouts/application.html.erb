<!DOCTYPE html>
<html>
	<head>
		<title>RQDB</title>
		<%= csrf_meta_tags %>
		<%= csp_meta_tag %>

		<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
		<%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
	</head>

	<body>
		<div class="container">
			<div class="row col-md-8" id="title">
				<h1>RQDB</h1>
			</div>
			<%= render 'quotes/navbar' %>
			<div class="col-md-8 quotes">
				<%= yield %>
			</div>
			<%= render 'quotes/navbar' %>
			<div class="row col-md-8" id="versionbar">
				RQDB Version: pre-alpha
			</div>
		</div>
	</body>
</html>
